# 2026-06-23 (6)

## Trabajo realizado
- **Refactor ScoringEngine + Commands**:
  - Añadido `Prediction::fromModel(PredictionModel)` para convertir Eloquent → dominio
  - Añadido `StageResult::fromRow(\stdClass)` para convertir resultado DB → dominio
- **ScoreStageCommand reescrito**:
  - Usa domain entities en lugar de Eloquent directamente (fix type mismatch)
  - Agrupa predicciones por league_id para cargar scoring system una sola vez
  - Eager loading de relaciones para evitar N+1
  - Flag `--force` para re-score: borra eventos existentes y recalcula
  - Sin `--force`: detecta si ya hay score events y omite (idempotente)
- **ScorePreRaceCommand nuevo** (`race:score-pre-race`):
  - Puntúa predicciones pre-race de ediciones finalizadas
  - Acepta edition_id opcional (todas si se omite)
  - Estructura preparada para cuando lleguen resultados GC reales
- **RebuildScoresCommand reescrito**:
  - Ahora BORRA score_events existentes y los recalcula desde cero
  - Procesa todas las ligas o una específica
  - Para cada stage finalizado, re-ejecuta el scoring engine
- **Tests** (14 nuevos):
  - ScoreStageCommand: 6 tests (stage no encontrado, sin resultados, scoring correcto, idempotencia, force flag, sin predicciones)
  - RebuildScoresCommand: 4 tests (liga no encontrada, rebuild específico, sin stages finalizados, rebuild todas)
  - ScorePreRaceCommand: 4 tests (sin ediciones, edición finalizada, edición específica, edición no finalizada)
- **128 tests pasando** (114 previos + 14 nuevos)

## Próximos pasos
- API externa real (CyclingDataFetcher)
- Resultados GC para scoring pre-race
