# 2026-06-24

## Trabajo realizado

### Bandera de países
- Instalada librería `country-flag-icons`
- Creado componente `FlagIcon` que resuelve dinámicamente la bandera por código ISO
- Integrado en listados: Riders Index, Teams Index/Show, Competitions Index/Setup
- Se muestran como SVG en línea (3x2 aspect ratio), ~54 kB gzip

### Listado de etapas (Stages Index)
- `StageController@index` ahora renderiza `Stages/Index` en vez de redirigir
- Paleta de colores por estado: finalizadas (gris), en curso (accent), próximas (blanco)
- Muestra puntos conseguidos por etapa (vía `score_events.stage_id`)
- Indicador visual de pronóstico guardado (`FileCheck` verde / `FileX` gris)
- Tests actualizados: 3 redirects reemplazados por aserciones Inertia

### Iconos de tipo de etapa
- Creado componente `StageTypeIcon` con SVGs inline:
  - `flat`: línea recta
  - `hill`: curva suave ascendente
  - `mountain`: pico triangular
  - `high_mountain`: doble pico
  - `time_trial`: cronómetro
  - `team_time_trial`: cronómetro con "E" (equipos)
  - `rest`: líneas + pausa
- Integrado en Stages Index/Show y Admin Stages Index/Show
- Admin StageController pasa `type_value` para los iconos

### Score events por etapa
- Migration: `stage_id` (UUID, nullable, index) en `score_events`
- `ScoreEvent` dominio: nuevo campo `?string stageId`
- `ScoringEngine::calculateStageScore`: acepta `?string $stageId`
- `ScoreStageCommand`: persiste `stage_id` al crear eventos

### Mejoras en visor de etapa (Stages Show)
- Pronóstico guardado se carga automáticamente al abrir la etapa
- Botón de guardar con color de estado: verde (guardado), ámbar (sin guardar), normal (nuevo)
- Ref interno `savedSnapshotRef` para detectar cambios sin re-render completo
- Badge "Guardado" / "Sin guardar" en cabecera

### Duplicados en pronósticos
- `stage_leader` y `stage_combativo` pueden repetir corredores del top 3
- Solo se bloquean entre sí (no mismo corredor en líder y combativo)

## Tests
- 128 tests pasando (531 assertions)
- Build frontend: 0 errores TypeScript
- Migraciones ejecutadas sin errores

## Próximos pasos
- API externa de ciclismo (CyclingDataFetcher) para importación automática
- Subida real a S3/configuración de bucket
- Asignar corredores reales y equipos vía importación CSV/API
