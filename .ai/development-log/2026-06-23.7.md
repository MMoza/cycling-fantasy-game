# 2026-06-23 (7)

## Trabajo realizado

### Branding y UI minimalista
- Paleta de colores cycling: navy (`brand-600`) + ámbar (`accent-500`) sobre base neutra
- CSS variables actualizadas con tonos azulados sutiles
- `tailwind.config.js`: colores personalizados `brand` y `accent`
- `ApplicationLogo.tsx`: nuevo SVG minimalista (círculo con icono de bicicleta estilizado)
- Todas las páginas pulidas con barras de color en cards, iconos teñidos, estados vacíos con iconografía
- Páginas pulidas: Dashboard, Leagues/Show, Stages/Show, Classification/Index, Predictions/PreRace

### SuperAdmin Portal
- Migration: `is_admin` boolean en `users`
- Middleware `EnsureIsSuperAdmin` (404 si no admin)
- Rutas `/admin/*` con middleware `super-admin`
- 5 controladores admin: Dashboard, Competition, Edition, Stage, User
- CRUD completo: crear/editar competiciones, ediciones, etapas
- Subida de imagen de perfil de etapa (Storage local, preparado para S3)
- AdminLayout con sidebar de navegación
- 9 páginas Inertia admin: Dashboard, Competitions (Index+Form), Editions (Index+Form), Stages (Index+Form), Users

### Perfil de etapa
- Migration: `elevation_gain` (int) y `profile_image` (string) en `stages`
- Domain entity `Stage` actualizada con `elevationGain` y `profileImage`
- `StageModel` actualizado (fillable + casts)
- `StageController@show` pasa nuevos campos al frontend
- Frontend: muestra imagen de perfil (con max-height), badge de desnivel
- Admin form: upload de imagen con preparación para S3 (usa `Storage` facade)

### Perfil de usuario migrado a AppLayout
- `Profile/Edit.tsx` ahora usa `AppLayout` en lugar de `AuthenticatedLayout`
- Consistente con el resto de la app

### Admin Panels: Teams, Riders, Competition Setup, Stage Results
- Migraciones: `teams`, `riders`, `team_rosters`, `competition_participants`
- Modelos Eloquent: TeamModel, RiderModel, TeamRosterModel, CompetitionParticipantModel
- Admin/TeamController: CRUD equipos + gestión de plantillas por temporada (año)
- Admin/RiderController: CRUD corredores
- Admin/CompetitionSetupController: asignar equipos y corredores a competiciones por edición
- Admin/StageController mejorado: cambiar estado (upcoming → finished), añadir resultados con posiciones
- Frontend: Teams (Index, Form, Show con roster), Riders (Index, Form), Setup (gestión participantes), Stage Show (resultados)
- Admin nav sidebar actualizado con secciones Equipos y Corredores

### Tests
- 128 tests pasando (sin regresiones)
- Build frontend: 0 errores TypeScript, compilación exitosa
- Migraciones ejecutadas sin errores

## Próximos pasos
- API externa de ciclismo (CyclingDataFetcher) para importación automática
- Subida real a S3/configuración de bucket
- Asignar corredores reales y equipos vía importación CSV/API
