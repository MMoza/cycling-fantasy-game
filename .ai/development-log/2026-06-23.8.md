# 2026-06-23 (8)

## Trabajo realizado

### Dificultad de etapa (1-3 estrellas)
- Migration: `difficulty` (unsignedTinyInteger, nullable) en `stages`
- Domain entity `Stage` actualizada con `difficulty`
- `StageModel` actualizado (fillable + casts)
- Admin form: selector de dificultad con estrellas amarillas
- Admin Index: muestra `★` según dificultad
- Admin Show: nuevo grid de 4 cards (recorrido, distancia, desnivel, dificultad)

### Validación por tipo de competición
- Clásicas (classic): solo 1 etapa permitida, error si se intenta crear más
- `StageController@store`: verifica `CompetitionType::Classic` y cuenta etapas existentes
- Frontend: nota informativa en el formulario para clásicas

### Fix UI: selects transparentes
- `SelectTrigger`: cambiado de `bg-transparent` a `bg-background` + `hover:bg-accent`
- Los selects ahora tienen fondo visible y feedback hover

### Fix UI: UUID en vez de label en selects
- Root cause: Base UI no resuelve labels automáticamente sin el prop `items`
- `SelectValue` mejorado con prop `format: (value) => ReactNode`
- Actualizados 4 archivos con UUID-based selects (competición, sistema, riders, equipos)

### Padding en estados vacíos
- Añadido `px-6` o `px-4` a 8 estados vacíos que perdían padding horizontal

## Tests
- 128 tests pasando (sin regresiones)
- Build frontend: 0 errores TypeScript, compilación exitosa
- Migraciones ejecutadas sin errores

## Próximos pasos
- API externa de ciclismo (CyclingDataFetcher) para importación automática
- Subida real a S3/configuración de bucket
- Asignar corredores reales y equipos vía importación CSV/API
