# 2026-06-24 (tarde)

## Trabajo realizado

### Podium de etapa con resultados reales
- `StageController@show` ahora devuelve `stage_results` (posiciones, nombres, imágenes, gap, flags) y `stage_classification` (puntos por usuario en la etapa)
- Nuevo componente `StageResultsCard` con:
  - Podium 2-1-3 con fotos de corredores, medallas, tiempo
  - Iconos Crown (líder GC) y Flame (supercombativo) sobreimpresionados con fondo blanco en la imagen
  - Listado posiciones 4+ con miniaturas, expandible "Ver más / Ver menos"
  - Mismos iconos en el listado y al pie centrados sin texto
- La card de resultados sin borde

### Admin: flags GC leader y Combativo
- Migración `add_gc_leader_and_combativo_to_stage_results` con columnas booleanas
- `Admin/StageController@show`: pasa `is_gc_leader` e `is_combativo` a la vista
- `Admin/StageController@storeResult`: valida y guarda ambos campos
- UI: botones toggle Crown/Flame por fila en el formulario de resultados

### Listado de etapas rediseñado
- Nuevo layout por card: título + estrellas de dificultad (fondo negro, estrellas amarillas), origen→destino, fecha hora | distancia | desnivel
- Etapa en curso resaltada (anillo accent), finalizadas con opacidad reducida
- Solo icono de pronóstico guardado (FileCheck verde / FileX gris), sin texto
- Etapas finalizadas muestran trofeo + puntos
- Añadido `elevation_gain` y `scheduled_start` al index

### Hora de inicio UTC
- `scheduled_start` en index y show
- Mostrado en listado (junto a fecha) y en stat card "Salida"
- Backend ya valida que no se puedan guardar predicciones tras `scheduled_start`

### Domain
- `StageResult` entidad: añadidos `isGcLeader` y `isCombativo`
- `StageResultModel`: fillable y casts boolean para ambos campos
- Tests unitarios actualizados

## Tests
- 130 tests pasando (563 assertions, +1 test nuevo)
- 0 errores TypeScript en build
- Pint: CS aplicado a 22 archivos

## Próximos pasos
- API externa de ciclismo (CyclingDataFetcher) para importación automática
- Subida real a S3/configuración de bucket
- Asignar corredores reales y equipos vía importación CSV/API
