# 2026-06-23 (2)

## Trabajo realizado
- **Navegación**: Añadidos "Etapa" y "Clasificación" al menú principal en AppLayout
  - Solo visibles cuando el usuario tiene una liga activa (`currentLeague` desde Inertia shared data)
  - Iconos: Route (Etapa), Trophy (Clasificación)
- **Migration**: `scheduled_start` datetime nullable en tabla `stages` (hora de salida que define cierre de apuestas)
- **StageController**:
  - `index()`: redirige a etapa en curso/próxima no finalizada de la liga activa
  - `show()`: detalle de etapa + navegación anterior/siguiente + predicciones del usuario
  - Comparte `navigation` (prev/next stage) y `all_stages` para navegación interna
- **PredictionController**:
  - `store()`: guarda predicciones pre_stage (stage_winner, stage_second, stage_third, stage_leader, stage_combativo)
  - Validación: bloquea si `scheduled_start` ya pasó
  - Usa `updateOrCreate` para permitir re-guardado
- **ClassificationController**:
  - `index()`: leaderboard real desde ScoreEventModel agrupado por usuario
  - Ranking ordenado por puntos totales descendente
  - Muestra detrás del líder, medalla de oro al 1º
- **Rutas nuevas**:
  - `GET /leagues/{league}/stage` → stages.index
  - `GET /leagues/{league}/stage/{stage}` → stages.show
  - `POST /leagues/{league}/stage/{stage}/predict` → predictions.store
  - `GET /leagues/{league}/classification` → classification.index
- **HandleInertiaRequests**: Comparte `currentLeague` (id + name) desde `last_visited_league_id`
- **StageModel**: Añadido `scheduled_start` a fillable y casts
- **League/Show**: Stages ahora enlazan a `stages.show`, incluyen `id` en datos
- **Frontend**: `Stages/Show.tsx` con navegación prev/next, formulario de pronósticos, estado locked si ya empezó
- **Frontend**: `Classification/Index.tsx` con tarjeta de posición del usuario y tabla de posiciones
- **81 tests pasando** (31 feature + 50 unit)
- **Build**: compilación correcta con Vite

## Problemas encontrados
- Ninguno relevante

## Próximos pasos
- Añadir tests para StageController, PredictionController, ClassificationController
- Implementar sistema de cierre automático de apuestas (comando artisan)
- Integrar API externa para resultados reales
