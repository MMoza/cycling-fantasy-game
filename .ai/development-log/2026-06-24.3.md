# 2026-06-24 (noche)

## Trabajo realizado

### Corrección de errores en predicciones
- `Prediction::fromModel`: normaliza `prediction_value` string a `['rider_id' => ...]` para legacy data
- `PredictionController::store` y `storePreRace`: guardan valores como array en vez de string plano
- Frontend resuelve UUIDs a nombres de corredores vía `riderMap` en ambas páginas (Show, PreRace)

### Motor de puntuación completo con dificultad
- `ScoringSystemSeeder`: 29 reglas para sistema "Estándar Tour" con puntuación por dificultad (1★/2★/3★)
- `ScoringEngine`: métodos `calculateStageScore(difficulty)`, `calculateGcTop5Score`, `calculateJerseyScore`, `calculateSimpleScore`
- `ScoringRule`: añadidos campos `difficulty` (1-3) y `position` (1-5)
- `ScoringRuleType`: añadidos tipos parciales para Top 5 y podios de maillots
- Admin `storeResult` valida y auto-puntúa al guardar resultados
- Commands: `race:score-stage`, `race:rebuild-scores` actualizados con dificultad

### Clasificaciones finales (pre-race scoring)
- Migración `final_classifications` para almacenar resultados reales de edición (GC Top 5, podios de maillots, equipos, supercombativo)
- `FinalClassificationModel` + `FinalClassificationController` admin
- UI admin con selectores de corredor/equipo por categoría
- `race:score-pre-race` completado: lee clasificaciones, usa ScoringEngine, persiste score_events
- `race:rebuild-scores` actualizado para incluir pre-race

### Flujo de puntuación completo
1. Admin guarda resultados de etapa → `scoreStage()` → `score_events` (etapa)
2. Admin introduce clasificaciones finales → `final_classifications`
3. Se ejecuta `race:score-pre-race` → `score_events` (pre-race, opcionalmente automático al finalizar edición)
4. `race:rebuild-scores` recalcula todo

## Tests
- 131 tests pasando (572 assertions)
- 0 errores TypeScript
- Build limpio
- Pint clean

## Próximos pasos
- Pre-race prediction frontend en página de liga (GC Top 5, maillots, equipos, supercombativo)
- API externa de ciclismo para importación automática
- Asignar corredores reales y equipos
